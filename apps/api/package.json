{
  "name": "@quan/api",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "build": "wrangler deploy --dry-run",
    "deploy": "wrangler deploy",
    "db:migrate": "wrangler d1 execute quan-marketing-db --file=./src/db/schema.sql"
  },
  "dependencies": {
    "hono": "^4.4.0",
    "nanoid": "^5.0.7"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20240529.0",
    "wrangler": "^3.60.0",
    "typescript": "^5.4.0"
  }
}
